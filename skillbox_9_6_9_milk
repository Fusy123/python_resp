'''Задача 9. Коровы
Для коров есть 10 стойл. В каждом стойле разные условия для животных, поэтому и молока
они дают по-разному. В первом стойле производят 2 литра в день, во втором 4, в третьем - 6,
потом 8, 10, 12, 14, 16, 18, 20. Но коровы стоят не во всех стойлах. Свободные и занятые
обозначаются строкой из букв a и b, где a - свободное стойло, b - занятое.
Пользователь вводит строку из 10 символов a и b. Необходимо определить, сколько в итоге
будет произведено молока за день.'''
import re

milk = 0
while True:
    cow_box = input('Введите последовательность из 10 символов (a и b): ')
    if len(cow_box) != 10 or cow_box.isdigit():
        print('Введите корретное значение!')
        continue

    cow_box2 = re.findall(r'a|b', cow_box) ()
    if len(cow_box2) != 10:
        print('Введите корретное значение!')
        continue
    else:
        for pos, box in enumerate(cow_box):
            if box == 'a':
                print('| |', end='')
            elif box == 'b':
                milk = milk + ((pos + 1) * 2)
                print('|cow|', end='')
        break
print('\n')
print('Молока = ', milk, 'литров')
